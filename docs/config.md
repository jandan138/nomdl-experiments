# 配置文件说明

`src/config.py` 用于集中管理路径与随机种子等全局设置，方便后续脚本共享。

- `DATA_ROOT`：原始图像所在的根目录。
- `FOLDER_A` / `FOLDER_B`：分别指向原材质（A）与简化材质（B）的文件夹名称。
- `OUTPUT_ROOT` 及子目录：实验结果（图表、表格、特征）会保存在这些路径下，脚本会按需自动创建。
- `RANDOM_SEED`：控制随机过程（例如 t-SNE）以保证多次运行结果一致性。

如需修改文件组织结构，只需调整这里的值，其余代码会自动适配。